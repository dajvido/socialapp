%h1 Listing users

%table
  %thead
    %tr
      %th Avatar
      %th Login
      %th Email
      %th

  %tbody
    - @users.each do |user|
      %tr
        %td= image_tag user.avatar.url(:thumb)
        %td= user.login
        %td= user.email
        %td= link_to 'Show', user

